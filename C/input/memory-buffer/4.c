#include <stdio.h>
int main()
{
    char a, b;

    // input

    scanf("%c", &a); // read from memory buffer
    scanf("%c", &b); // read from memory buffer

    // output

    printf("%c, %c\n", a, b);

    return 0;
}
