#include <stdio.h>
int main()
{
    // Declare a string
    char str[100];

    // Input string from user using gets()
    gets(str);

    // Output string using puts()
    puts(str);

    return 0;
}
