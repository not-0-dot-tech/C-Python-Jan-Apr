#include <stdio.h>
int main()
{
    int a, b, c;

    // input

    // scanf("%d", &a);
    // scanf("%d", &b);
    // scanf("%d", &c);

    // scanf("%d %d %d", &a, &b, &c); // this line is equivalent to the above three lines
    scanf("%d%d%d", &a, &b, &c); // this line is equivalent to the above line

    // output

    printf("%d %d %d\n", a, b, c);

    return 0;
}
