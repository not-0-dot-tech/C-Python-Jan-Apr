#include <stdio.h>

int main()
{
    // -----------------------------------
    if (0) // always false
        printf("The is always false.\n");
    // -----------------------------------
    printf("This is always printed.\n"); // no condition is applied
    printf("This is also always printed.\n"); // no condition is applied
    return 0;
}
