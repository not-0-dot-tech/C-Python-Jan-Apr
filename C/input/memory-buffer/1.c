#include <stdio.h>
int main()
{
    int a, b;

    // input

    scanf("%d", &a); // read from memory buffer
    scanf("%d", &b); // read from memory buffer

    // output

    printf("%d %d\n", a, b);

    return 0;
}
