#include <stdio.h>

int main()
{
    // always true
    if (1) // if (78) --> any non zero number is true
        printf("The is always true.\n");

    // always false
    if (0)
        printf("The is always false.\n");

    return 0;
}

