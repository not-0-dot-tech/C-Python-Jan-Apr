#include <stdio.h>

int main()
{
    int arr[5]; // static array

    /*
        doing something with the array
        for example, filling it with values
        for (int i = 0; i < 5; i++)
        {
            arr[i] = i + 1;
        }
        printf("Array values: ");
    */

    int arr[7]; // trying to redeclare the array with a different size

    return 0;
}
